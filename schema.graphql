type Query {
  currentUser: User

  allFeeds: [Feed!]!
  feed(id: ID!): Feed!
}

type Mutation {
  addFeed(feed: FeedInput!): Feed!
  refreshFeed(id: ID!): Feed!
}

type Feed {
  id: ID!
  url: String!
  title: String!
  homePageURL: String!
}

input FeedInput {
  url: String!
}

type User {
  name: String!
  nickname: String!
  picture: String!
}
